service: leaguify-backend

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  stage: ${opt:stage, 'dev'}
  environment:
    NODE_ENV: ${self:provider.stage}
    DB_SSL: 'true'
    AVATAR_BUCKET_NAME: ${cf:${self:provider.stage}-leaguify-backend-stack.AvatarBucketName, ''}
    AWS_REGION: ${self:provider.region}
  vpc:
    securityGroupIds:
      - ${cf:${self:provider.stage}-main-stack.LambdaSecurityGroupId}
    subnetIds:
      - ${cf:${self:provider.stage}-main-stack.PrivateSubnet1Id}
      - ${cf:${self:provider.stage}-main-stack.PrivateSubnet2Id}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - ec2:CreateNetworkInterface
            - ec2:DescribeNetworkInterfaces
            - ec2:DeleteNetworkInterface
          Resource: '*'
        - Effect: Allow
          Action:
            - ssm:GetParameter
            - ssm:GetParameters
          Resource:
            - arn:aws:ssm:${self:provider.region}:*:parameter/${self:provider.stage}/leaguify/db/*
            - arn:aws:ssm:${self:provider.region}:*:parameter/${self:provider.stage}/leaguify/avatars/*
        - Effect: Allow
          Action:
            - s3:PutObject
            - s3:GetObject
            - s3:DeleteObject
          Resource:
            - ${cf:${self:provider.stage}-leaguify-backend-stack.AvatarBucketArn, ''}/*
        - Effect: Allow
          Action:
            - s3:ListBucket
          Resource:
            - ${cf:${self:provider.stage}-leaguify-backend-stack.AvatarBucketArn, ''}

functions:
  api:
    handler: src/lambda.handler
    timeout: 30
    memorySize: 512
    events:
      - http:
          path: /{proxy+}
          method: ANY
          cors: true
      - http:
          path: /
          method: ANY
          cors: true

plugins:
  - serverless-offline

custom:
  serverless-offline:
    httpPort: 3001
