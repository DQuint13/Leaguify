# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=leaguify
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# Server Configuration
PORT=3001

# Environment
NODE_ENV=development

# AWS Configuration (for Lambda deployment)
# These will be set automatically when deployed to AWS
# AWS_REGION=us-east-1
